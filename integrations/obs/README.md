# OBS integration (planned)

This directory reserves the adapter boundary for OBS Studio through obs-websocket. There is no adapter implementation yet.

The adapter will expose connection health, current scene, guarded scene changes, and media-source control. Scene names come from OpenIRL configuration; credentials come from the environment.
