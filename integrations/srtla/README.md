# SRTLA integration

Receiver-side integration for bonded SRTLA contribution links.

The first milestone will document the receiver process, map each listener to a stable MediaMTX feed path, expose link statistics, and define graceful shutdown. SRTLA is treated as an ingest transport, not as a feed identity.
